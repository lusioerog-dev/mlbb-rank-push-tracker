# Focused tracker production release — 15 September 2026

Checkpoints C, D and E are deployed to the stable production site:
https://mlbb-rank-push-tracker.pages.dev/.

## Data safety and migrations

- A fresh production export matched revision 8 before any write. Its PostgreSQL
  content checksum was `ca9bea153aa4fc134d5e07ff0587c5e9`; the preserved JSON
  file SHA-256 was
  `40C0FBB59570BF0AD8F26536A86572A8F411068BCE6524A81AE997FD3FA532D8`.
- Restore and v2 migration rehearsals reproduced the source exactly: two members,
  three matches, nine history records, 103 Mythical Immortal stars, three wins
  and 4,889 recorded seconds.
- `202609140004_match_identity.sql` and
  `202609140005_rank_checkpoints.sql` were applied in order.
- The post-migration inventory remained revision 8 with two members, one season,
  one active season, three matches and nine history records. It contained two
  heroes, one unambiguous normalized rank target and zero invented checkpoints.

The recovery source is `research-private/backup-2026-09-14-rev8.json`. Legacy
tables remain available. Never restore this snapshot over newer production
matches; after new writes, prefer a forward repair or a verified reverse export.

## Deployment and verification

- Application source checkpoint: `a051066` (`feat: focus tracker performance views`).
- Cloudflare Worker version:
  `ed916f19-693f-44e2-9032-08ea790480f5`.
- Cloudflare Pages deployment:
  `https://37012cdf.mlbb-rank-push-tracker.pages.dev` promoted to the stable URL.
- The API rejected an unauthenticated v2 read with HTTP 401 and retained CORS for
  the stable Pages origin.
- Signed-in production verification loaded the real shared account at 103 stars,
  three wins and two players. Overview, Hero Performance and Lane Performance
  rendered without console errors. Hero totals showed two Benedetta games and
  one Aulus game.
- Existing matches predate the position field, so Lane Performance correctly
  shows “No position data yet” until a match is recorded or corrected with its
  played position.

Before deployment, all 46 automated tests, TypeScript checks, ESLint, Prettier
and the production build passed. The build retains Vite's existing large chunk
warning; it does not prevent the release.

Checkpoint F, the reviewed importer, is optional and was not included. Official
hero portraits and game artwork remain future presentation work.
